export interface AppColor {
  name: string;
  accent: string;
  glow: string;
  gradient: string;
}

export interface AppData {
  name: string;
  description: string;
  url: string;
  video: string;
  color: AppColor;
}
